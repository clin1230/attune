# Copy to .env.local and fill in locally, or set process environment variables.
# The backend loads .env.local; process environment values take precedence.
OPENAI_API_KEY=
OPENAI_MODEL=
BLENDER_PATH=/Applications/Blender.app/Contents/MacOS/Blender
